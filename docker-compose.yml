---
version: '3.7'
services:
  server:
    build: .
    command: python -m certifire.manage runserver
    hostname: certifire
    ports:
      - 5000:5000
    volumes:
      - .:/usr/src/app
    restart:
      always
